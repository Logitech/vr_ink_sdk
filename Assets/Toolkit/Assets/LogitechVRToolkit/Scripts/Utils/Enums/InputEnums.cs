namespace Logitech.XRToolkit.Utils
{
    /// <summary>
    /// Button events for buttons that have rising edge, on and falling edge states.
    /// </summary>
    public enum EButtonEvent
    {
        OnButtonDown,
        OnButton,
        OnButtonUp
    }
}
